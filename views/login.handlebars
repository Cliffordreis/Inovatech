{{#if error}}
<div class="alert alert-danger col-10 col-sm-8 col-md-8 col-lg-5 d-flex align-items-center mx-auto mt-3" role="alert" style="position: relative;">
    <i class="bi bi-exclamation-triangle-fill me-2"></i>
    <div>
        {{error}}
    </div>
</div>
{{/if}}

<div class="container-fluid d-flex justify-content-center my-5" data-bs-theme="dark">
        <div id="cad" class="card col-10 col-sm-8 col-md-6 col-lg-4 p-3 text-center">
            <form action="/entrando" method="post">
                <div class="mb-5">
                    <h1 class="home">Login</h1>
                </div>

                <input class="form-control mb-2" type="email" name="email" placeholder="digite seu email" required>
                <input class="form-control mb-2" type="password" name="senha" placeholder="digite sua senha" required>
                <a class="home" href="/cadastro"><p>não tem conta? cadastre-se aqui</p></a>
                <button class="btn btn-transparent">entrar</button>
            </form>
        </div>
</div>
